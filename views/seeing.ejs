<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Blog Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/blog.css">
</head>
<body>

    <div class="container">
        <div class="blog-container">
            <h2 class="mb-4">Latest Blog Posts</h2>

            <!-- Loop through blog posts -->
            <% if (locals.heading) { %>
                <div class="p-3 border rounded mb-3">
                    <p class="blog-title"><%= heading %></p>
                    
                    <div class="d-flex justify-content-end">
                        <!-- Edit Button -->
                        <a href="/edit" class="btn btn-success mx-3" method="Get">Edit</a>
                        
                        <!-- Delete Button -->
                        <form action="/submit-form" method="POST">
                            <button type="submit" class="btn btn-success">Submit</button>
                        </form>
                    </div>
                </div>
            <% } else { %>
                <p class="text-center text-muted">No blog posts yet. Start writing!</p>
            <% } %>

            <!-- Return to Landing Page -->
            <div class="text-center">
                <a href="/" class="btn btn-secondary">Return to Landing Page</a>
            </div>
        </div>
    </div>

</body>
</html>
